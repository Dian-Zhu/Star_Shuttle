应用锁在用户锁屏时应该也进行触发
隧道功能和转发功能单独放到一个按钮
新建连接页面中，把保存连接换成连接，用户点击后开始连接并保存连接
系统快捷键不生效
通用设置中的主题和外观内容重复，移除主题设置
通用设置中添加快捷键设置，显示所有快捷键和用户可以自定义设置
新增历史选项卡，点击后显示最近连接的历史记录
新增系统资源监控窗口，显示CPU、内存、磁盘等系统资源使用情况
新增标签功能，用户可以创建标签分列，新建连接添加的标签，自动分入列表中，方便用户管理和查找


1. 应用锁功能增强：
   - 实现当设备锁屏时自动触发应用锁功能
   - 确保应用锁与系统锁屏无缝衔接，提供一致的安全体验
   - 添加设置选项允许用户配置锁屏触发行为

2. 功能按钮重组：
   - 将隧道功能和转发功能从新建连接界面分离
   - 创建专用功能按钮"高级"集中管理这两个功能
   - 保持原有功能完整性的同时优化用户界面布局

3. 连接页面改进：
   - 将"保存连接"按钮重命名为"保存并连接"
   - 实现点击后自动执行连接操作并保存连接配置
   

4. 快捷键问题修复：
   - 全面检查系统快捷键绑定机制
   - 修复导致快捷键失效的根本问题
   - 添加快捷键冲突检测和提示功能

5. 设置界面优化：
   - 移除通用设置中重复的主题设置选项
   - 保留并完善外观设置功能
   - 确保界面修改后设置项逻辑清晰无冗余

6. 快捷键管理功能：
   - 在通用设置中添加"快捷键设置"面板
   - 显示当前所有可用快捷键列表
   - 实现用户自定义快捷键功能
   - 添加快捷键冲突检测和恢复默认功能

7. 历史记录功能：
   - 新增"历史记录"选项卡
   - 按时间倒序显示最近连接记录
   - 实现点击历史记录快速重连功能
   - 添加历史记录搜索和筛选功能

8. 系统监控窗口：
   - 新增系统资源监控面板
   - 实时显示CPU使用率、内存占用、磁盘IO等关键指标
   - 提供资源使用历史图表
   - 添加资源告警阈值设置

9. 标签管理系统：
   - 实现连接标签创建和管理功能
   - 支持多级标签分类
   - 新建连接时可添加多个标签
   - 提供基于标签的快速筛选和搜索功能
   - 确保标签数据与连接配置同步保存