应用锁在用户锁屏时应该也进行触发
隧道功能和转发功能单独放到一个按钮
新建连接页面中，把保存连接换成连接，用户点击后开始连接并保存连接
系统快捷键不生效
通用设置中的主题和外观内容重复，移除主题设置
通用设置中添加快捷键设置，显示所有快捷键和用户可以自定义设置
新增历史选项卡，点击后显示最近连接的历史记录
新增系统资源监控窗口，显示CPU、内存、磁盘等系统资源使用情况
新增标签功能，用户可以创建标签分列，新建连接添加的标签，自动分入列表中，方便用户管理和查找


1.4 
关闭日志中的debug信息
设置中通用设置的快捷键设置和快捷键设置功能重复，移除其中一个
终端设置中增加滚动行数设置，增加光标样式
外观设置中增加更多的主题和外观样式
ctrl+shift+p打开的界面中的快捷键与实际快捷键不符，不能使用
新建连接中的高级功能和主页面上的高级按钮的功能重复，移除高级按钮

1.5
切换侧边栏快捷键不可以
修改分组逻辑，通过标签进行分组，不需要单独设立一个分组选项。当用户输入的标签没有分组目录时，自动创建。
保存连接存在问题，勾选保存密码后，也未保存密码，每次点击都需要重新输入密码
实时流量功能显示数值没有变化
原有系统监控功能按钮不变，在页面最下方，实时流量旁增加简略的系统资源展示
最下方的时间，并无变化
当前文件浏览器中没有拖拽下载和拖拽上传功能
实现一个可以直接双击打开远程文件的功能，并能直接修改保存